const initalState = {
  isLoading: false,
  isError: false,
  Doctors: [],
};
export const reducer = (state = initalState, { type, payload }) => {
  switch (type) {
    default:
      return state;
  }
};
